#==============================================================================
# Testing Requirements (Phase 4+)
#==============================================================================
# Install after Phase 1 setup works:
#   pip install -r requirements.txt  # Install Phase 1 first
#   pip install -r requirements-testing.txt  # Then add testing deps

#Sigma rule validation (Phase 4)
pysigma
pysigma-backend-elasticsearch
pysigma-pipeline-sysmon

#ELK integration testing (Phase 5)
elasticsearch
docker
pytest

#additional testing utilities
pytest-asyncio
pytest-cov
